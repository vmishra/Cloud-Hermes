# Generated by Cloud Hermes — review before applying.
resource "google_compute_subnetwork" "{name}" {
  name          = "{name}"
  network       = "{network}"
  region        = "{region}"
  ip_cidr_range = "{range}"
}
