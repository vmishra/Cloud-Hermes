# Generated by Cloud Hermes — review before applying.
resource "google_compute_network" "{name}" {
  name = "{name}"
  # subnet mode: {subnetMode} — custom is recommended
  auto_create_subnetworks = false
  routing_mode            = "REGIONAL"
}
