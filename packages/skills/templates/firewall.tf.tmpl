# Generated by Cloud Hermes — review before applying.
resource "google_compute_firewall" "{name}" {
  name      = "{name}"
  network   = "{network}"
  direction = "{direction}"

  # protocol and ports from: {rules}
  allow {
    protocol = "tcp"
  }

  source_ranges = ["{sourceRanges}"]
}
